# Example

Demonstrates the `colored_image` package with interactive color selection and multiple use cases.

## Running

```bash
cd example
flutter pub get
flutter run
```

## What's Included

- Single icon coloring with `ColoredImage`
- Multiple widgets with `ColorTintFilter`
- Gradient preservation examples
- Interactive color picker with 16 colors
